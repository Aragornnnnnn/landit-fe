NEXT_PUBLIC_API_BASE_URL=백엔드_API_주소
# 표현학습 API mock (선택) — enabled면 MSW가 /api/v1/expressions/* 를 가로채 mock 응답을 준다. BE 준비되면 지운다
NEXT_PUBLIC_API_MOCKING=enabled
# 개발용 로그인 우회 노출 (선택) — true면 로그인 화면에 우회 버튼이 뜬다. OAuth 없이 보호 화면 확인용
NEXT_PUBLIC_ENABLE_DEV_LOGIN=true

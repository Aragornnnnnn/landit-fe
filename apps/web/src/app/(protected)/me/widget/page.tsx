// 홈 화면 위젯 설치 안내(/me/widget) — 마이페이지에서 다시 여는 자리. 안내 흐름 자체는 온보딩과 같은 InstallGuide다
import { WidgetGuideScreen } from './_ui/WidgetGuideScreen';

export default function WidgetGuidePage() {
  return <WidgetGuideScreen />;
}

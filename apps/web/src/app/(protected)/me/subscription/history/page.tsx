// 결제 내역(/me/subscription/history) — BE가 RevenueCat 웹훅으로 쌓은 이력을 최신순으로 보는 화면
import { SubscriptionHistoryScreen } from './_ui/SubscriptionHistoryScreen';

export default function SubscriptionHistoryPage() {
  return <SubscriptionHistoryScreen />;
}

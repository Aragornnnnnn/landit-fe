// 피드백 유형 선택(/mailbox/compose) — 무슨 말을 하러 왔는지 먼저 고른다
import { FeedbackTypeFlow } from '@/features/mailbox/ui/FeedbackTypeFlow';

export default function FeedbackTypePage() {
  return <FeedbackTypeFlow />;
}

---
name: capture
description: landit 웹 화면을 PNG 파일로 찍는다 — PR 이미지, 스토어·피그마 시안, 보고서, 문서용. 헤드리스 크롬(playwright-core)으로 375×812@2(PR)나 390×832@3(피그마 폰 목업) 프리셋, 가짜 세션 주입, API 목(route), dev 오버레이 숨김, 가짜 마이크, 여러 장 흐름 촬영, HTML 도식→PNG, dev 서버 없이 컴포넌트만 찍기까지. "캡처해줘", "스크린샷 떠줘", "PR에 넣을 이미지", "전후 비교 이미지", "피그마에 올릴 화면", "도식 그려서 PNG로", "이 화면 찍어서 보여줘" 같은 요청에 사용한다. 브라우저 패널 스크린샷은 파일이 아니라 검증용이므로, 파일이 필요하면 이 스킬을 쓴다.
---

# 화면 캡처

## 어떤 방법을 쓰나

목적과 환경으로 고른다. 대부분 첫째다.

| 상황                                                            | 방법                                                                                                                  |
| --------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| 화면을 파일로 (PR·피그마·스토어·보고서)                         | **`scripts/capture.mjs`** — dev 서버 + 헤드리스 크롬. 아래 절차.                                                      |
| 도식·합성(전→로딩→후)                                           | HTML 한 장을 쓰고 같은 스크립트로 `file://` 촬영.                                                                     |
| dev 서버를 못 띄움 / 로그인·API 목이 번거로움 / 컴포넌트 하나만 | vitest로 렌더해 DOM 덤프 → `globals.css`를 `@tailwindcss/postcss`로 컴파일해 물림 → `file://`로 촬영.                 |
| 동작 확인만 (파일 불필요)                                       | 브라우저 패널. 단 패널은 rAF가 멈춰 motion 애니메이션이 얼어붙고 클릭이 안 먹을 때가 있다. 코드 버그로 오인하지 마라. |

파일이 필요한데 패널 스크린샷으로 대신하려 하지 마라. 파일이 아니다.

## 절차 (기본 방법)

**1. 서버.** 캡처할 브랜치의 워크트리에서 dev 서버를 띄운다. 브라우저 패널의 프리뷰 슬롯이 차 있으면 Bash 백그라운드로 다른 포트에 띄운다.

```bash
pnpm --filter web exec next dev -p 3124
```

카카오 로그인 리다이렉트는 3000만 등록돼 있다. 가짜 세션으로 찍을 때는 포트가 상관없고, 실제 로그인이 필요할 때만 3000이어야 한다.

**2. 데이터.** 세 가지 중 하나.

- **가짜 세션 + API 목** (기본) — `--auth`로 세션을 넣고, `--steps` 파일의 `setup`에서 `ctx.route('**/api/**', …)`로 응답을 준다. `setup`은 첫 이동 전에 돌아서 화면이 처음 뜰 때 나가는 요청부터 목이 걸린다. 토큰이 필요 없고 상태를 마음대로 만든다. 로딩 장면은 route 핸들러가 응답을 붙들고 있는 동안 찍는다(`unroute` 하지 마라 — "Route is already handled" 에러. 같은 주소를 다시 잡으면 새 핸들러가 우선).
- **dev 서버 실데이터** — 사용자가 dev 토큰을 줬을 때만. `--steps`의 `setup`에서 `ctx.addInitScript`로 `/api/` 요청에 Authorization을 붙인다. 프로덕션 토큰을 잡아내는 fetch 패치는 하지 않는다. 프로덕션 콘텐츠가 필요하면 사용자 크롬(Claude in Chrome)의 로그인 세션에서 DOM(`innerText`, `img src`)만 읽어 목 데이터에 옮긴다.
- **컴포넌트만** — 로그인 벽 뒤 컴포넌트를 상태별로 찍을 때는 임시 라우트 `app/(public)/dev-<이름>/page.tsx`를 만들어 상태 라디오로 전환한다. **커밋 금지.** `.git/info/exclude`에 넣는다.

응답 봉투는 `{ success: true, data }`다. 봉투를 빼먹으면 화면이 빈다.

**3. 촬영.**

```bash
cd <스크래치패드> && npm i playwright-core   # 없으면 npx 캐시에서 자동으로 찾는다
node <스킬 경로>/scripts/capture.mjs --url http://localhost:3124/home --out ./home.png --auth --wait "text=오늘의 시나리오" --delay 2000
```

- 프리셋 — PR은 기본(375×812 @2). 피그마 폰 목업·스토어는 `--preset figma`(390×832 @3, 폰 SCREEN 958×2044 비율이라 크롭 없이 FILL, 상단 58px 스테이터스 자리 비움).
- 여러 장·클릭 흐름 — `--steps flow.mjs`. 파일은 두 부분이다. `export const setup = async ({ ctx, page }) => { … }`는 첫 이동 **전에** 돌아 목 라우트·`addInitScript`를 등록하고, `export default async ({ page, ctx, shot }) => { … }`는 이동 **뒤에** 돌아 클릭하고 여러 장 찍는다. 라우트를 default 쪽에 두면 첫 화면의 요청은 목 없이 나간다. 한 흐름을 한 파일에 두면 다음에 같은 화면을 다시 찍을 때 그대로 돌린다.
- 녹음 화면 — `--mic`. 가짜 장치라 무음 게이트를 통과한다. 애니메이션 중인 버튼은 `page.click(sel, { force: true })`.
- 긴 화면 — `--full`.

**4. 확인.** 찍은 파일을 Read로 열어 본다. 빈 화면·스켈레톤·오버레이가 찍혔으면 `--wait`·`--delay`를 조정한다. 애니메이션 등장(축하 2.6초, 분석 2초, 속마음 글자 2.6초 같은 것)은 그만큼 기다려야 한다.

**5. 전달.** PR이면 `pr` 스킬의 pr-assets 절차. 피그마면 `upload_assets`(응답에 `placedOnNodeId`가 없으면 fill이 안 붙은 것 — `imageHash`로 fills를 직접 지정). 사용자에게 보여 주려면 SendUserFile.

## 도식과 합성

상자·화살표 도식이나 "A → 로딩 → B" 합성은 HTML 한 장으로 만든다. 이미지는 data URI로 인라인. 그걸 같은 스크립트로 찍는다.

```bash
node <스킬 경로>/scripts/capture.mjs --url "file://$PWD/diagram.html" --out ./diagram.png --delay 200
```

폭은 HTML의 `body` 폭으로 정해진다. 프리셋 뷰포트보다 넓은 도식은 `--full`을 쓰거나 HTML에서 `body{width:…}`를 지정한다. 꾸미지 않는다. 상자·화살표·라벨이면 충분하다.

## 자주 걸리는 것

- **`networkidle`을 기다린다** — Next dev는 HMR 소켓 때문에 안 온다. 스크립트는 `load`까지만 기다린다. 나머지는 `--wait`·`--delay`.
- **온보딩·코치마크가 뜬다** — `--auth`가 온보딩 완료 플래그를 넣는다. 스몰톡 안내·탭 코치마크는 `landit-smalltalk-intro-guide-seen`·`landit-smalltalk-tap-greeting-seen`을 `setup`의 `addInitScript`에서 넣는다. 반대로 그 화면을 찍으려면 지운다.
- **`member.provider`가 없다** — 프로필 게이트가 다시 묻는다. 스크립트의 가짜 세션에는 들어 있다.
- **데스크톱 뷰포트로 찍혔다** — 사용자 크롬으로 찍을 때 모바일 프리셋을 안 걸면 여백까지 찍혀 종횡비가 깨진다. 이 스크립트는 뷰포트를 고정하니 해당 없다.
- **스토어 캡처에 dev 시드 콘텐츠** — 반려된다. 스토어·마케팅용은 프로덕션 콘텐츠(실제 표현·시나리오 텍스트와 이미지)를 목 데이터에 옮겨 찍는다.
- **playwright MCP를 쓴다** — 다른 세션이 프로필을 잡고 있으면 못 쓴다. 이 스크립트가 더 안정적이다.
- **그림 원본이 사라졌다** — 스크래치패드는 세션마다 휘발한다. 다시 필요할 것 같으면 pr-assets나 `~/Downloads`에 두고, steps 파일을 같이 남겨 재촬영 가능하게 한다.

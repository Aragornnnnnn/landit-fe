# React Native 툴체인이 pnpm 심볼릭 링크 node_modules와 호환되지 않아 hoisted 방식 사용 (Expo 공식 권장)
node-linker=hoisted

# Next.js 16이 Node >=20.9.0을 요구함 (next/package.json engines 참고).
# 어차피 dev/build 시점에 터질 실패를 install 시점으로 앞당겨 가장 먼저 걸리게 함. (.nvmrc로 버전 유도 + 이걸로 차단)
engine-strict=true
